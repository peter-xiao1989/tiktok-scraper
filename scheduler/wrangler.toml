name = "tiktok-scraper-scheduler"
main = "worker.js"
compatibility_date = "2024-12-01"

# Single cron, fires every hour; the worker routes by the fired UTC hour (see
# worker.js): every hour → realtime.yml; 08 also → daily-product; 23 → daily-ads.
[triggers]
crons = ["0 * * * *"]

[vars]
REPO = "peter-xiao1989/tiktok-scraper"

# Secrets (do NOT put here — set with `wrangler secret put`):
#   wrangler secret put GH_PAT
#   wrangler secret put FEISHU_WEBHOOK
